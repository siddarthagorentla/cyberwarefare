# Server Configuration
PORT=5000

# MongoDB Connection
# Option 1: Local MongoDB (uncomment if using local)
# MONGODB_URI=mongodb://localhost:27017/course-subscription

# Option 2: MongoDB Atlas (recommended - create free cluster at mongodb.com/atlas)
# Replace <username>, <password>, and <cluster-url> with your Atlas credentials
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/course-subscription?retryWrites=true&w=majority

# JWT Secret (change in production)
JWT_SECRET=blackfriday2024supersecretkey

# Promo Code Configuration
PROMO_CODE=BFSALE25
PROMO_DISCOUNT=50
